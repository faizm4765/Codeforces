#include<bits/stdc++.h>
using namespace std;

int go(int n, int m, string &x, string &s){
    

    return j + 1;
}

int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);
    int n, m;
    cin >> n >> m;

    string x;
    cin >> x;

    string s;
    cin >> s;

    cout<<go(n, m, x, s)<<endl;
    return 0;
}